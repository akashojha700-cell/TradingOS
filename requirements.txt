# ---------------------------------------------------------------------------
# TradingOS - Python runtime dependencies
# Pin lower bounds; CI resolves upper compatibility.
# Keep this list lean. Only add libraries actually used by application code.
# ---------------------------------------------------------------------------

# Web framework + ASGI server
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Data validation & settings
pydantic==2.10.3
pydantic-settings==2.6.1

# Database
SQLAlchemy==2.0.36

# HTTP client (used by future provider adapters)
httpx==0.28.1

# Structured logging
structlog==24.4.0

# Environment / configuration
python-dotenv==1.0.1

# ---- Dev / test ----------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
